[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wsl-dash"
version = "0.1.0"
description = "Serve data produced inside WSL to Windows desktop widgets"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = []
